# PR — {기능}  ·  브랜치 `{branch}`  →  `{base}`

> 자율 TDD 하네스 산출. 사람은 이 PR 하나로 **진행·결과물·증거**를 확인하고 **머지를 승인**한다. (자동 머지 없음 — `git.auto_merge: false`)

## 1. 요약
- 기능/계약: {기능}  ·  목표 동작 승인: {승인 기록 링크/SHA}
- 주행 결과: {무인 완주 / 개입 N회 / 에스컬레이션 N건}
- 레벨: 단위 {n} · 통합 {n} · E2E {n}  (3레벨 필수 충족)

## 2. 감사 판정 (tdd-auditor)
- **판정: PASS | PASS-WITH-NITS | FAIL**
- 정합성 매트릭스: 전략↔테스트 {✅} · 3레벨 존재 {✅} · RED 정직성 {✅} · 테스트 동결 {✅} · 리팩터 품질 {✅} · CI {✅}
- NITS(🟡 권장 / 🟢 참고): {목록 또는 '없음'}

## 3. 게이트 통과표 (locked-evaluator · 결정적)
| 게이트 | 결과 | 근거(로그/SHA) |
|--------|:---:|----------------|
| 1 RED (assertion) | ✅ | `gate_red.txt` |
| 2 테스트-먼저·동결 | ✅ | test SHA < impl SHA · `git diff src/test` |
| 3 GREEN·유지 | ✅ | |
| 4 객관 룰(있으면) | ✅ / — | `arch_cmd` |
| 5 커버리지 바닥 | ✅ | line/branch (기본·고위험) |
| 6 고위험 동시성 | ✅ / — | |
| 7 잠긴 목표 동작 | ✅ | `_target_behavior` E2E |
| 8 심층(고위험 자동) | ✅ / — | `mutation_cmd` |

## 4. 커밋 = 증거 (R/G/R 별도, 작업 브랜치)
| 증분 (api/layer) | RED | GREEN | REFACTOR |
|------------------|-----|-------|----------|
| {api}/{unit} | {sha} | {sha} | {sha} |
| {api}/{integration} | {sha} | {sha} | {sha} |
| {api}/{e2e} | {sha} | {sha} | {sha} |

## 5. 신뢰 원장 요약
- 증분 {N} · 자동복구(새 RED) {N} · 에스컬레이션 {N} · 사람 개입 {N}
- 전체 원장: `trust-ledger.md` (이 브랜치에 동봉)

## 6. 변경 (diff)
- 변경 파일 요지 / `git diff {base}...{branch}` 링크
- 신규·삭제(객관 룰 제거로 삭제된 게이트 등): {목록 또는 '없음'}

## 7. 미해결 · 후속
- 에스컬레이션 미해결: {없음 / 내용 + 필요한 사람 결정}
- 파괴적 마이그레이션 승인 필요: {없음 / 범위}
- Coherence 리팩터 RED 제안(다음 사이클): {없음 / 목록}

---
### 머지 가이드 (사람)
- 승인 → **사람이 머지** (base 반영).
- 변경요청 → 같은 브랜치에서 하네스 재실행 → 추가 커밋 → 이 PR 자동 갱신 → 재리뷰.
